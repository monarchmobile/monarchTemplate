<h1><%= @blog.title %></h1>
<%= raw @blog.body %>
<%= link_to 'Edit | ', edit_blog_path(@blog) if can? :edit, @blog %>
<%= link_to 'Back', blogs_path if can? :edit, @blog %>

<h2>Comments</h2>
<%= render @comments %>
<%= render 'comments/form' %>
